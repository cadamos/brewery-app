import { SpecBreweryPipe } from './spec-brewery.pipe';

describe('SpecBreweryPipe', () => {
  it('create an instance', () => {
    const pipe = new SpecBreweryPipe();
    expect(pipe).toBeTruthy();
  });
});
